#include <bits/stdc++.h>
using namespace std;

#define PI acos(-1.0)
#define ll long long
#define vi vector<int>
#define pb push_back
#define return_loser return 0;

const int N = 1e5 + 10;

class Account
{
private:
    double balance;
    string password; // data hiding
public:
    string accountId;
    string username;

    // member function / method
    void updateBalance(double newBalance)
    {
        balance = newBalance;
    }
};

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    // there are four pilar in OOPs 1. encapsulation 2. abstraction 3. inheritance 4. polymorphism
    // encapsulation is wrapping up of data/attribute & member function in a single unit called class.

    return_loser;
}